# Token-Rate-Limit-Filter
Limits non-admin users to a configurable token budget per rolling time window (in minutes). Admins are exempt. Counts only NEW user input + assistant output (not full chat context).
